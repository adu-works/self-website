# v1.1 检查记录

2026-09-16，原型检查，不代表用户审美验收或生产验收。

## 已执行

- JavaScript 语法检查：app.js、scene.js、terrain.mjs。
- terrain.test.mjs：沿坡前到坡后的连续移动测试，高差超过 3.4 单位；同时检查上升、下降、炉体/飞船阻挡与地图边界。最初测试门槛写成 3.5，实际叠加微起伏后的峰值约 3.49，因此改为验证设计需要的显著高差 3.4，非修改实现掩盖穿模。
- 本地静态入口 HTTP 200；浏览器无错误级运行日志。
- 桌面完整故事：开炉门→三次独立锻打→取铲→辅助沿地形行走→五次独立铲土→开箱→验证假配置→收船票→走到飞船→开舱→登船→发射→跳过飞行抵达 Chat。
- WebMCP 状态回读：骨骼模型已加载，行走中的 position.y 等于 groundHeight；坡后到达状态保留铲子，动作未自动完成挖掘。
- 另一条路：搬起熔炉→砸下→主站。
- Chat：答案内引用跳至 article/sources/section-1，DOM 确认高亮 section-1；返回保留两条消息；新建会话后可恢复旧会话。
- 管理回归：生成提案→取消，显示没有变更或发布。完整发布/恢复异常矩阵沿用 v1.0，本轮没有重复声称全量回归。
- 手机 390×844：桌面 document scrollWidth=390，无横向溢出；Chat 配置完成、底部输入区可见、会话/设置菜单可展开和收起。测试后已恢复浏览器视口。
- 参考站实际回访：Shar Work 打开窗口；Alana Projects 打开文件夹但列表当时为加载占位，Notes/消息窗口可见；Greg 本次 Application Error。

## 修正的问题

1. 导入角色的初始包围盒和中立动画姿态差异巨大，造成角色缩小。先播放中立姿态并重算 SkinnedMesh 包围盒再确定比例。
2. 远景全景图初次作为平面会露边，改为天空背景，远山由连续地形提供。
3. 删除 v1.0 自动打造/挖掘分支与 E 快捷推进，避免旧逻辑残留。
4. 手机隐藏侧栏原本会藏住配置/历史，新增展开入口。

## 明确限制

- 写实方向的砂砾材质与光影；不是摄影级地形、电影级角色或完整物理模拟。没有逐脚 IK，石块未逐颗碰撞。
- 辅助移动是有约束的直线路径，不是任意障碍寻路。两条主路径已走通。
- 未执行真实设备 GPU 性能、全屏浏览器矩阵、正式读屏/200% 放大验收。
- AI、模型验证、登录、发布与恢复仍为模拟；没有网络模型质量与安全验收。

## v1.1.1 · 放下与快捷键

- 新增 R 放下熔炉、X 砸下、F 搬起、E 当前动作、G 辅助移动，按钮显示按键提示。
- 放下动画回到炉体原位置并恢复碰撞；应用层回到搬起前阶段，保留 strikes 与炉门状态。搬起状态暂停移动，避免炉体位置与碰撞原点脱节。
- 已执行 app.js/scene.js 语法检查、shortcuts.test.mjs（上下文映射、R/X 分离、按住重复、输入法、组合键、阻塞输入）以及原地形测试，全部通过。
- 本次没有新增浏览器动作回归；放下动画实际观感待体验，不把代码检查写成浏览器验证。

## v1.1.2 · 握持、负重与归航

- 执行 JavaScript 语法检查，以及 terrain、shortcuts、rig-flight 三个测试文件，4 个测试全部通过。
- 骨骼测试加载实际 astronaut.glb：30 个步行采样中，手部插槽保持挂接，叠加搬运手臂姿态不会改变髋部世界位置。测试不证明手指抓握或逐脚 IK 已完善。
- 本地浏览器实际 F 搬起→触控向右移动，回读 walking=true 且角色位置变化→R 在新位置放下→E 打开炉门继续锻造。画面确认站立持炉，不再跪地。
- 实际完成 3 次锻打、取铲、沿坡移动、5 次铲土、开箱、假配置、开舱、登船和发射；回读 shovelAttached=true、gripError=0，画面确认工具在手上。
- 观察火箭地球轨道、下降、舱门与出舱、伸展镜头，最终完整 Chat 自动出现。首次观察发现伸展仍拿铲子、实心机身遮住舱口，修正后重看确认空手出舱、可见开口与落地阴影。大气层阶段顺序有时间线检查；没有保存完整逐帧录屏。
- Chat 的“重看归航”完整运行后再次进入 Chat；重看中点击跳过也回到 Chat。本次没有重复全部问答/管理矩阵，相关逻辑保留。
- 浏览器错误级日志为空。本轮桌面检查；未重新执行手机真实 GPU/帧率验收。低帧率会延长演绎时长。
- 当前放下恢复的是新位置碰撞，替代 v1.1.1 临时原位放下/禁走。程序化双臂和云层/热辉为原型演绎，非电影级资产或完整物理。

## v1.1.3 · 连续动作与导航

- `node --test *.test.mjs`：11 项通过。新增历史返回/前进、引用链、深链接后备、动作曲线起止、中立姿态、镜头衔接、未绑定动画关节的重复采样与动作退出复位。
- 浏览器实际核对的页面来源、返回、取消与状态保留详见上级 navigation-audit-v1.1.3.md；未逐项实测与手机限制也在表内注明。
- 本轮确认首页详情返回首页、筛选项目列表往返保留“线索”、文章列表返回、浏览器前进、引用段落高亮及返回保留消息/草稿、详情进入 Chat 后返回详情、直接锚点没有伪造返回 Chat、配置 Esc、清除配置地址一致、未配置 Chat 有出口、关于/联系返回、工作台输入/提案/执行结果恢复、恢复预览取消回记录、无效路由回主站。
- 实际观察两次独立铲土，回读握持误差为 0、动作模式为 dig。归航观察出舱侧绕至肩后、伸展及视线推进，最终回完整 Chat。
- 观察中发现未被模型动画关键帧重写的关节发生旋转累加；改为每帧基准姿态并检查 120 次重复采样。首次角度断言受导入四元数微小非单位长度影响，改为归一化后比较角度，未放宽误差阈值。动作结束也显式恢复手臂。
- 演绎时间使用真实经过时间，物理移动保留步长上限，避免 UI 动作完成先于动画收势；扬尘触发按跨越接触时间判断。
- 浏览器错误级日志为空；未执行真实手机 GPU、全量读屏与逐帧录屏。没有声称动作捕捉级观感或整阶段审美验收。

## v1.1.4

`node --test *.test.mjs`：12 项通过。新增检查覆盖视线段摄像机与头部距离、切换点的离散机位变化、共同视线目标。已有握持、动作复位、导航、快捷键和地形检查继续通过。浏览器重播采样观察抬臂、落臂/肩后以及 Chat 渐显；非逐帧运动捕捉级验收。原型人物模型仍为风格化资源。

## v1.1.5

- `node --check dist/app.js`、`node --check dist/detail-views.js` 通过；12 项既有回归检查通过。
- 桌面实际观察火星案例封面、案例章节、手记、关于和联系；目录跳转可用。
- 390px 检查文章及索引项目，DOM 宽度均为 390px，无横向溢出；已针对索引窄纸卡调整字号。
- `article/sources/section-1` 保持高亮，焦点为 section-1，目标在视口内。下一篇进入对应文章；关于→联系→返回确实回关于。
- 真实个人内容仍待提供。未把截图采样或自动回归描述为用户审美验收。

## v1.1.6

两个修改模块语法检查通过；12 项既有自动回归通过。浏览器验证姓名链接进入社媒页；七平台空地址均为不可点击占位。真实外链因未提供尚未验证。

## 2026-09-17 目录清理验证

当前源码迁到 prototype/，22 个运行文件 SHA-256 与迁移前一致。12 项自动回归通过；61 个文档相对链接、24 个静态入口/相对模块引用检查通过；新目录本地 HTTP 入口响应成功。原型代码和 Site 运行行为未改动，未重复发布。
